const myNumber = 3;                   // a number
console.log("myNumber =", myNumber, "it's type is:", typeof myNumber);

const myString = "Hello, World!"      // a string
console.log("myString =", myString, "it's type is:", typeof myString);

const myBoolean = true;               // a boolean
console.log("myBoolean =", myBoolean, "it's type is:", typeof myBoolean);

const myArray = [
  1,
  "3",
  {foo: "bar"}
];                    // an array
console.log("myArray =", myArray, "it's type is:", typeof myArray);

const myObject = {
  qaTeam: {
    adrian: "https://github.com/unexpected-qa"
  }
};                  // an object
console.log("myObject =", myObject, "it's type is:", typeof myObject);

function myFunction() {
};                  // an object
console.log("myFunction =", myFunction, "it's type is:", typeof myFunction);

const emptyVariable = null;
console.log("emptyVariable =", emptyVariable, "it's type is:", typeof emptyVariable);

const undefinedVariable = undefined;
console.log("undefinedVariable =", undefinedVariable, "it's type is:", typeof undefinedVariable);